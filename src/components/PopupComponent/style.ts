import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
popoverText: {
    padding: 10,
    fontSize: 16,
},
popoverHeader: {display: "flex", alignItems: "center", flexDirection: "row", justifyContent: "space-between", width: "100%", paddingHorizontal: 5}
})

export default styles